var moment=require("../../moment");exports.utc={"utc and local":function(e){e.expect(7);var a=moment(Date.UTC(2011,1,2,3,4,5,6));a.utc(),e.equal(a.date(),2,"the day should be correct for utc"),e.equal(a.day(),3,"the date should be correct for utc"),e.equal(a.hours(),3,"the hours should be correct for utc"),a.local(),a.zone()>180?(e.equal(a.date(),1,"the date should be correct for local"),e.equal(a.day(),2,"the day should be correct for local")):(e.equal(a.date(),2,"the date should be correct for local"),e.equal(a.day(),3,"the day should be correct for local"));var o=Math.ceil(a.zone()/60),n=(27-o)%24;e.equal(a.hours(),n,"the hours ("+a.hours()+") should be correct for local"),e.equal(moment().utc().zone(),0,"timezone in utc should always be zero"),e.done()},"creating with utc":function(e){e.expect(7);var a=moment.utc().valueOf()-moment().valueOf();a=Math.abs(a),e.ok(5>a,"Calling moment.utc() should default to the current time");var o=moment.utc([2011,1,2,3,4,5,6]);e.equal(o.date(),2,"the day should be correct for utc array"),e.equal(o.hours(),3,"the hours should be correct for utc array"),o=moment.utc("2011-02-02 3:04:05","YYYY-MM-DD HH:mm:ss"),e.equal(o.date(),2,"the day should be correct for utc parsing format"),e.equal(o.hours(),3,"the hours should be correct for utc parsing format"),o=moment.utc("2011-02-02T03:04:05+00:00"),e.equal(o.date(),2,"the day should be correct for utc parsing iso"),e.equal(o.hours(),3,"the hours should be correct for utc parsing iso"),e.done()},"creating with utc without timezone":function(e){e.expect(4);var a=moment.utc("2012-01-02T08:20:00");e.equal(a.date(),2,"the day should be correct for utc parse without timezone"),e.equal(a.hours(),8,"the hours should be correct for utc parse without timezone"),a=moment.utc("2012-01-02T08:20:00+09:00"),e.equal(a.date(),1,"the day should be correct for utc parse with timezone"),e.equal(a.hours(),23,"the hours should be correct for utc parse with timezone"),e.done()}};